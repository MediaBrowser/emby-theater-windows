<UserControl x:Class="MediaBrowser.Theater.DefaultTheme.Login.Views.ServerConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:MediaBrowser.Theater.DefaultTheme.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <controls:TileButton x:Name="Button" Width="400" Height="400" Margin="10" Command="{Binding ConnectCommand}">
        <controls:TileButton.Header>
            <TextBlock Text="{Binding Name}" Margin="10 5 15 10"
                               Foreground="{Binding Foreground, ElementName=Button}" />
        </controls:TileButton.Header>

        <!-- todo server icon -->
        <Grid Background="#CFCFCF" Opacity="0.6"/>
    </controls:TileButton>
</UserControl>
