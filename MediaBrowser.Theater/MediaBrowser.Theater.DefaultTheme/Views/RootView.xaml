<UserControl x:Class="MediaBrowser.Theater.DefaultTheme.Views.RootView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:MediaBrowser.Theater.Presentation.Controls;assembly=MediaBrowser.Theater.Presentation"
             xmlns:viewModels="clr-namespace:MediaBrowser.Theater.DefaultTheme.ViewModels"
             mc:Ignorable="d" 
             Foreground="{StaticResource ForegroundBrush}"
             d:DataContext="{d:DesignInstance viewModels:RootViewModel}"
             d:DesignHeight="480" d:DesignWidth="720">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <controls:DragBar Grid.Row="0"/>
        <controls:WindowCommands Grid.Row="0" ShowButtons="{Binding IsMouseIdle, Converter={StaticResource BooleanInverter}, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type controls:BaseWindow}}}" HorizontalAlignment="Right"/>
        
        <controls:ExtendedContentControl Grid.Row="1" Content="{Binding ActivePage}"/>
    </Grid>
</UserControl>
