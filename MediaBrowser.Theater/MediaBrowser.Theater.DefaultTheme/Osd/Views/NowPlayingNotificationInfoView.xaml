<UserControl x:Class="MediaBrowser.Theater.DefaultTheme.Osd.Views.NowPlayingNotificationInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="38" d:DesignWidth="200">
    <Grid>
        <ProgressBar Grid.Row="0" Height="NaN" Minimum="0" Maximum="{Binding DurationTicks}" Value="{Binding ProgressTicks, Mode=OneWay}"
                     Visibility="{Binding IsInProgress, Converter={StaticResource BooleanToVisibility}}"
                     VerticalAlignment="Stretch"
                     HorizontalAlignment="Stretch"
                     Background="Transparent"
                     Style="{StaticResource ResumeProgressBar}"/>

        <TextBlock Text="Series Name S01E01" FontSize="26" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" MaxWidth="300" Margin="4"/>
    </Grid>
</UserControl>
