<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:MediaBrowser.Theater.Presentation.Controls">

    <Style TargetType="{x:Type controls:ExtendedContentControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ExtendedContentControl}">
                    <Grid>
                        <ContentPresenter x:Name="PART_TransitionContentPresenter" Content="{x:Null}" ContentTemplate="{TemplateBinding ContentTemplate}" />
                        <ContentPresenter x:Name="PART_ActiveContentPresenter" Content="{x:Null}" ContentTemplate="{TemplateBinding ContentTemplate}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>