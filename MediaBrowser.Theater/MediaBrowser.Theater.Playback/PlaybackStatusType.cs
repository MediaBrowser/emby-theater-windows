namespace MediaBrowser.Theater.Playback
{
    public enum PlaybackStatusType
    {
        Idle,
        Started,
        Playing,
        Paused,
        Complete,
        Skipped,
        Error,
        Stopped
    }
}